<%= form_with(model: user, local: true) do |form| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
        <% user.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :username, class: 'py-2' %>
    <%= form.text_field :username, required: true, class: 'pl-2' %>
  </div>

  <div class="field">
    <%= form.label :fullname, class: 'py-2' %>
    <%= form.text_field :fullname, required: true, class: 'pl-2' %>
  </div>

  <div class="actions">
    <%= form.submit 'Create User and Sign In', class: 'btn btn-success p-0 px-3' %>
  </div>
<% end %>
